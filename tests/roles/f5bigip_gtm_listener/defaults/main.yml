---

listener_name: my_listener
listener_partition: Common
listener_description: My listener
listener_address: 10.10.1.1
listener_persist:
  - { name: dest_addr, partition: Common, tmDefault: 'yes' }
listener_source_address_translation:
  type: automap

listener_profiles:
  - { name: dns, partition: Common, context: all }
